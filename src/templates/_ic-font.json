{ <% _.each(glyphs, function(glyph) { %>
<% if (_.isEqual(glyph.name, _.last(glyphs).name)) { %>	"<%= glyph.name %>": "\<%= glyph.unicode[0].charCodeAt(0).toString(16).toUpperCase() %>"
<% } else { %>	"<%= glyph.name %>": "\<%= glyph.unicode[0].charCodeAt(0).toString(16).toUpperCase() %>",<% }}); %> }
